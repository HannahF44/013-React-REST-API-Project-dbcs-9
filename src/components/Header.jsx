import React from 'react';
import './Header.css'
export default function Header({ title, typeTitle }) {
  return (
    <header>
      <h1><a href="#">{title}</a></h1>
      <nav>
        <ul>
          <a href="#">Home</a>
        </ul>
      </nav>

    </header>
  );

    <header>
      <h1><a href="#">{typeTitle}</a></h1>
      <nav>
        <ul>
          <a href="#">Home</a>  <c href="#">Dark</c>
        </ul>
      </nav>

    </header>
  
}

